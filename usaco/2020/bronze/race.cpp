#include <iostream>

using namespace std;

int main() {
    int race_length;
    int N;
    int end_speeds[N];

    cin >> race_length >> N;

    for (int i = 0; i < N; i++)
    {
        cin >> end_speeds[i];
    }

    return 0;
}